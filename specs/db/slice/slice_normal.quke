feature Slice a plain table.

  before
    import "db";
    `PlainTable set ([] c1:1 2 3 4 5);

  after
    delete PlainTable from `.;

  should slice a plain table by given start and end indices
    expect a slice of the table within the given start and end indices
      .qu.compare[.qtk.db.slice[`PlainTable; 2; 4]; ([] c1:3 4)]
