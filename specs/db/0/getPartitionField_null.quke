feature Get partition field.

  before
    import "db";

  should give partition field of a partitioned database.
    expect partition field of the current database.
      .qu.compare[.db.getPartitionField[]; `]
