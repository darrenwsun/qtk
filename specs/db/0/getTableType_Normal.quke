feature Get table type of Plain.

  before
    import "db";

  after
    delete T from `.;

  should return `Plain` for an in-memory table.
    expect `Plain` for an in-memory table passed by value.
      .qu.compare[.qtk.db.getTableType ([] c1:1 2); `Plain]

    expect `Plain` for an in-memory table passed by name.
      `T set ([] c1:1 2);
      .qu.compare[.qtk.db.getTableType `T; `Plain]
