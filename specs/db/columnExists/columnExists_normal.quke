feature Check if a column exists in a plain table.

  before
    import "db";
    import "tbl";
    `PlainTable set ([] c1:1 0; c2:10b);

  after
    delete PlainTable from `.;

  should return `1b` if a column exists.
    expect `1b`.
      .qu.compare[.qtk.db.columnExists[`PlainTable; `c1]; 1b]

  should return `0b` if a column doesn't exist.
    expect `0b`.
      .qu.compare[.qtk.db.columnExists[`PlainTable; `c3]; 0b]
