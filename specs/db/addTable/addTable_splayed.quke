feature Add a splayed table.

  before each
    import "db";
    .qutils.hdbRoot:"/tmp/hdb_addTable_splayed";
    system "cd ",.qutils.hdbRoot;
    .db.addTable[`SplayedTable; ([] c1:1 0; c2:10b); `Splayed];
    system "l ",.qutils.hdbRoot;

  after each
    system "rm -rf ",.qutils.hdbRoot;

  should add a splayed table.
    expect the table added.
      .qu.compare[select from SplayedTable; ([] c1:1 0; c2:10b)] and
        .qu.compare[.db.getTableType `SplayedTable; `Splayed]
