feature Drop a serialized table.

  before each
    .qtk.import.loadModule["tbl";`qtk];
    .qutils.hdbRoot:"/tmp/hdb_drop_serialized";
    .qtk.tbl.create[hsym `$.qutils.hdbRoot,"/SerializedTable"; ([] c1:("aa";"bb"); c2:`a`b)];

  should drop a serialized table.
    expect the serialized table dropped.
      .qu.result:.qtk.tbl.drop[`SerializedTable];
      .qu.compare[key `SerializedTable; ()]
