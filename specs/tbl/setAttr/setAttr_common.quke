feature Add attribute to a column.

  before each
    .qtk.import.loadModule["tbl";`qtk];
    .qtk.tbl.create[`PlainTable; ([] c1:1 2)];

  after each
    delete PlainTable from `.;

  should raise error "ColumnNotFoundError: c2" if `column` doesn't exist.
    expect raise error "ColumnNotFoundError: c2" if `column` doesn't exist.
      .qu.compare[
        @[.qtk.tbl.setAttr[`PlainTable; ; `p]; `c2; {x}];
        "ColumnNotFoundError: c2"]
