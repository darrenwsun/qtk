feature Add attribute to a column of a plain table.

  before each
    .qtk.import.loadModule["db";`qtk];
    .qtk.import.loadModule["tbl";`qtk];
    .qtk.tbl.create[`PlainTable; ([] c1:1 2)];

  after each
    delete PlainTable from `.;

  should add an attribute to a column of a plain table.
    expect the attribute added to the column.
      .qtk.tbl.setAttr[`PlainTable; `c1; `p];
      .qu.compare[(meta[PlainTable]`c1)`a; `p]
