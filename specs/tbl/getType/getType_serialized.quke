feature Get table type of a serialized table.

  before each
    .qtk.import.loadModule["tbl";`qtk];
    .qutils.hdbRoot:"/tmp/hdb_getType_serialized";
    .qutils.tabRef:hsym `$.qutils.hdbRoot,"/SerializedTable";
    .qtk.tbl.create[.qutils.tabRef; ([] c1:1 2)];

  after each
    .qtk.tbl.drop .qutils.tabRef;

  should return `Serialized` for a serialized table.
    expect `Serialized` for a serialized table specified by hsym.
      .qu.compare[.qtk.tbl.getType .qutils.tabRef; `Serialized]
